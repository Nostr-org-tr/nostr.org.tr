---
import Layout from '../../layouts/Layout.astro';
import BreadcrumbSchema from '../seo/BreadcrumbSchema.astro';
import SoftwareApplicationSchema from '../seo/SoftwareApplicationSchema.astro';
import FaqSchema from '../seo/FaqSchema.astro';
import { nostrTools, type ToolItem } from '../../data/tools';
import { 
  ShieldCheck, 
  Sparkles, 
  HelpCircle, 
  ArrowRight, 
  CheckCircle2, 
  BookOpen, 
  ChevronRight,
  Lock
} from 'lucide-astro';

interface Props {
  tool: ToolItem;
}

const { tool } = Astro.props;

const breadcrumbs = [
  { name: 'Ana Sayfa', path: '/' },
  { name: 'Araçlar', path: '/araclar' },
  { name: tool.shortTitle, path: tool.href },
];

const relatedTools = nostrTools.filter((t) => t.id !== tool.id).slice(0, 3);
---

<!-- Hallmark · genre: editorial · macrostructure: Workbench · design-system: design.md · designed-as-app -->
<Layout 
  title={`${tool.title} | Nostr Türkiye Araçları`}
  description={tool.summary}
>
  <Fragment slot="head">
    <BreadcrumbSchema items={breadcrumbs} />
    <SoftwareApplicationSchema 
      name={tool.title}
      description={tool.summary}
      url={tool.href}
    />
    <FaqSchema faqs={tool.faqs} />
  </Fragment>

  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-10 sm:py-14 space-y-10">
    
    <!-- Breadcrumb Nav -->
    <nav class="flex items-center gap-1.5 text-xs text-slate-500 dark:text-slate-400 overflow-x-auto py-1 font-mono" aria-label="Gezinme Yolu">
      <a href="/" class="hover:text-purple-700 dark:hover:text-purple-300 transition-colors flex-shrink-0">Ana Sayfa</a>
      <ChevronRight class="w-3.5 h-3.5 opacity-60 flex-shrink-0" />
      <a href="/araclar" class="hover:text-purple-700 dark:hover:text-purple-300 transition-colors flex-shrink-0">Araçlar</a>
      <ChevronRight class="w-3.5 h-3.5 opacity-60 flex-shrink-0" />
      <span class="text-slate-900 dark:text-slate-200 font-semibold truncate">{tool.shortTitle}</span>
    </nav>

    <!-- Hero Header -->
    <header class="space-y-4">
      <div class="flex flex-wrap items-center gap-2">
        <span class="px-3 py-1 rounded-full text-xs font-bold uppercase tracking-wider bg-purple-50 dark:bg-purple-950/80 text-purple-700 dark:text-purple-300 border border-purple-200 dark:border-purple-800 font-mono">
          {tool.categoryLabel}
        </span>
        {tool.nips.map((nip) => (
          <span class="px-2.5 py-0.5 rounded-full text-xs font-mono font-medium bg-[#faf9f5] dark:bg-slate-800 text-slate-700 dark:text-slate-300 border border-slate-200 dark:border-slate-700">
            {nip}
          </span>
        ))}
        {tool.badge && (
          <span class="px-2.5 py-0.5 rounded-full text-xs font-semibold bg-emerald-50 dark:bg-emerald-950/60 text-emerald-700 dark:text-emerald-400 border border-emerald-200 dark:border-emerald-800 font-mono">
            {tool.badge}
          </span>
        )}
      </div>

      <div class="space-y-2">
        <h1 class="text-2xl sm:text-4xl font-extrabold tracking-tight text-slate-900 dark:text-white leading-[1.2]">
          {tool.title}
        </h1>
        <p class="text-base sm:text-lg text-slate-600 dark:text-slate-300 max-w-4xl leading-relaxed">
          {tool.description}
        </p>
      </div>

      <!-- Zero-Leak Client-Side Security Assurance Badge -->
      <div class="p-4 rounded-2xl bg-[#faf9f5] dark:bg-slate-900/90 border border-slate-200/90 dark:border-slate-800 flex items-center justify-between gap-3 text-xs shadow-2xs">
        <div class="flex items-center gap-2.5 text-slate-700 dark:text-slate-300">
          <div class="p-1 rounded-lg bg-emerald-100 dark:bg-emerald-950 text-emerald-700 dark:text-emerald-400">
            <ShieldCheck class="w-4 h-4" />
          </div>
          <span>
            <strong class="text-slate-900 dark:text-white">%100 İstemci Taraflı (Client-Side) Güvenlik:</strong> Tüm hesaplamalar, dönüştürmeler ve imzalamalar tarayıcınızda yerel çalışır. Hiçbir özel anahtar veya veri sunucuya iletilmez.
          </span>
        </div>
        <div class="hidden sm:flex items-center gap-1 font-mono text-[11px] text-slate-400 dark:text-slate-500">
          <Lock class="w-3 h-3" />
          <span>Yerel Çalışma</span>
        </div>
      </div>
    </header>

    <!-- Main Tool Container (Interactive Slot) -->
    <main class="space-y-8">
      <slot />
    </main>

    <!-- Features & Technical Breakdown -->
    <section class="p-6 sm:p-8 rounded-3xl bg-[#faf9f5] dark:bg-slate-900/60 border border-slate-200/90 dark:border-slate-800 shadow-xs space-y-6">
      <div class="flex items-center gap-2.5">
        <div class="p-2 rounded-xl bg-purple-100 dark:bg-purple-950/80 text-purple-700 dark:text-purple-300">
          <Sparkles class="w-5 h-5" />
        </div>
        <h2 class="text-lg sm:text-xl font-bold text-slate-900 dark:text-white">
          Özellikler ve Teknik Kapasite
        </h2>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-3.5">
        {tool.features.map((feat) => (
          <div class="flex items-start gap-2.5 p-3.5 rounded-xl bg-white dark:bg-slate-950 border border-slate-200/80 dark:border-slate-800/80 shadow-2xs">
            <CheckCircle2 class="w-4 h-4 text-emerald-600 dark:text-emerald-400 flex-shrink-0 mt-0.5" />
            <span class="text-xs sm:text-sm text-slate-700 dark:text-slate-300 font-medium leading-relaxed">
              {feat}
            </span>
          </div>
        ))}
      </div>

      <slot name="guide" />
    </section>

    <!-- FAQ Section -->
    {tool.faqs.length > 0 && (
      <section class="p-6 sm:p-8 rounded-3xl bg-[#faf9f5] dark:bg-slate-900/60 border border-slate-200/90 dark:border-slate-800 shadow-xs space-y-6">
        <div class="flex items-center gap-2.5">
          <div class="p-2 rounded-xl bg-amber-100 dark:bg-amber-950 text-amber-700 dark:text-amber-300">
            <HelpCircle class="w-5 h-5" />
          </div>
          <h2 class="text-lg sm:text-xl font-bold text-slate-900 dark:text-white">
            Sıkça Sorulan Sorular (SSS)
          </h2>
        </div>

        <div class="space-y-4">
          {tool.faqs.map((faq) => (
            <div class="p-4 sm:p-5 rounded-2xl bg-white dark:bg-slate-950 border border-slate-200/80 dark:border-slate-800 space-y-2 shadow-2xs">
              <h3 class="text-sm sm:text-base font-bold text-slate-900 dark:text-white flex items-center gap-2">
                <span class="w-1.5 h-1.5 rounded-full bg-purple-600 dark:bg-purple-400"></span>
                <span>{faq.question}</span>
              </h3>
              <p class="text-xs sm:text-sm text-slate-600 dark:text-slate-300 leading-relaxed pl-3.5">
                {faq.answer}
              </p>
            </div>
          ))}
        </div>
      </section>
    )}

    <!-- Related Tools Grid -->
    <section class="space-y-4 pt-4">
      <div class="flex items-center justify-between px-1">
        <div class="flex items-center gap-2">
          <BookOpen class="w-4 h-4 text-purple-600 dark:text-purple-400" />
          <h2 class="text-xs font-bold uppercase tracking-wider text-slate-500 dark:text-slate-400 font-mono">
            Diğer Nostr Araçları
          </h2>
        </div>
        <a 
          href="/araclar" 
          class="text-xs font-semibold text-purple-700 dark:text-purple-300 hover:underline flex items-center gap-1"
        >
          <span>Tüm Araçları Gör</span>
          <ArrowRight class="w-3.5 h-3.5" />
        </a>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
        {relatedTools.map((relTool) => (
          <a
            href={relTool.href}
            class="group p-5 rounded-2xl bg-[#faf9f5] dark:bg-slate-900 border border-slate-200/90 dark:border-slate-800 hover:border-purple-400 dark:hover:border-purple-600 transition-all shadow-2xs flex flex-col justify-between space-y-3"
          >
            <div class="space-y-2">
              <div class="flex items-center justify-between">
                <span class="text-[11px] font-bold font-mono text-purple-700 dark:text-purple-300 uppercase tracking-wider">
                  {relTool.categoryLabel}
                </span>
                <span class="text-[10px] font-mono px-2 py-0.5 rounded bg-white dark:bg-slate-800 text-slate-600 dark:text-slate-400 border border-slate-200/80 dark:border-slate-700/80">
                  {relTool.nips.join(', ')}
                </span>
              </div>
              <h3 class="text-sm font-bold text-slate-900 dark:text-white group-hover:text-purple-700 dark:group-hover:text-purple-300 transition-colors">
                {relTool.shortTitle}
              </h3>
              <p class="text-xs text-slate-500 dark:text-slate-400 line-clamp-2 leading-relaxed">
                {relTool.summary}
              </p>
            </div>
            <div class="flex items-center gap-1 text-xs font-semibold text-purple-700 dark:text-purple-300">
              <span>Aracı Aç</span>
              <ArrowRight class="w-3.5 h-3.5 group-hover:translate-x-1 transition-transform" />
            </div>
          </a>
        ))}
      </div>
    </section>

  </div>
</Layout>
